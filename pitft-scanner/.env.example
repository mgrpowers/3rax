# NATS Configuration
NATS_URL=nats://192.168.50.118:4222
NATS_SUBJECT=scanner.scans

# Scanner Configuration
SCANNER_ID=rpi-scanner-01

# Scanner device path (leave empty to auto-detect Zebra/Symbol scanners)
# Find yours with: python3 -c "import evdev; [print(d.path, d.name) for d in [evdev.InputDevice(p) for p in evdev.list_devices()]]"
SCANNER_DEVICE=
